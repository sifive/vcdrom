let processMessages = (message) => {
  // This was what was copied from ceg: return message.split(' ')[1];
};

module.exports = { processMessages };

//TODO modify this process message for different cases like delete graph etc
